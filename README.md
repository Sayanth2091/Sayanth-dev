# NULL_SECTOR

> `SUBJECT: S.S.` `STATUS: ACTIVE` `CLEARANCE: GRANTED`

Cinematic scroll-driven portfolio. Built on Astro 4, React Three Fiber, GSAP.

```
pnpm dev     // local dev on localhost:4321
pnpm build   // production build to dist/
pnpm preview // preview the build
```

`// view source` — you are already here.
